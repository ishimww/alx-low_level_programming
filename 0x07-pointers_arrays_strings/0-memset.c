#include "main.h"
/**
 * _memset - a function that fills memory with a constant byte
 * @s: memory address
 *@b: value to be filled
 *@n: number of bytes to be filled startin
 * Return: always zero.
 */
char *_memset(char *s, char b, unsigned int n)
{
	_memset(s, b, n);
	return (s);
}


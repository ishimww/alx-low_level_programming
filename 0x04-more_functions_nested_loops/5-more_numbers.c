#include "main.h"

/**
 * more_numbers - nmbr 
 * Return: no return
 */
void more_numbers(void)
{
	int a;
	int b;
	for (a = 1; a <=10; j++)
	{
		for (b = 0; b <= 14; b++)
		{
			_putchar(b);
		}
		_putchar('\n');
		Return (0);
	}
}	
